import fs from 'fs';import path from 'path';const R='music',O='tracks.json';function w(d){let r=[];for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);e.isDirectory()?r.push(...w(p)):r.push(p)}return r}const f=w(R).filter(x=>x.endsWith('.mp3')||x.endsWith('.m4a'));const t=f.map((x,i)=>({id:String(i),title:path.basename(x),artist:'Workout',file:x,section:x.split('/')[1]||'Other'}));fs.writeFileSync(O,JSON.stringify(t,null,2));